# ARneuro v2 base dependencies (Python >= 3.10)
# Install optional OCR/model/provider extras only for stages that need them.
pandas>=2.0
openpyxl>=3.1
numpy>=1.24
requests>=2.31
beautifulsoup4>=4.12
tqdm>=4.66
openai>=1.0
PyYAML>=6.0
python-dotenv>=1.0
Flask>=3.0

# PDF and citation text handling
pypdf>=3.0
pdfplumber>=0.10

# Development checks
pytest>=7.4
